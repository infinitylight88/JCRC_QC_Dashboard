library(shiny)

reports_server <- function(id,pool){
  
  moduleServer(
    
    id,
    
    function(input,output,session){
      
      ns <- session$ns
      
      # PDF logic later
      
    })
  
}